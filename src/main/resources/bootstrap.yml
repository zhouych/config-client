#spring.application.name=demo

#spring.cloud.config.profile=dev
#spring.cloud.config.label=master
#spring.cloud.config.uri=http://127.0.0.1:3000/config-svr/

#server.port=3300
#server.servlet.context-path=/${spring.application.name}

spring:
  application:
    name: demo
  cloud:
    config:
      profile: dev
      label: master
      uri: http://127.0.0.1:3000/config-svr/
      #uri: http://CONFIG-SVR/config-svr
    
server:
  port: 3300
  # servlet:
  #   context-path: /${spring.application.name}

eureka:
  client:
    service-url:
      defaultZone: http://127.0.0.1:8000/eureka/
  # 涉及到项目配置有server.servlet.context-path属性的相关处理
  # instance:
  #   metadata-map:
  #     management:
  #       context-path: ${server.servlet.context-path}/actuator
  #   health-check-url: http://127.0.0.1:${server.port}${server.servlet.context-path}/actuator/health
  #   status-page-url: http://127.0.0.1:${server.port}${server.servlet.context-path}/actuator/info
  #   home-page-url: http://127.0.0.1:${server.port}/